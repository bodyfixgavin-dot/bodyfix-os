"use client";
import { ClinicNotice, ClinicShell, useClinicFetch } from "@/components/clinic/ClinicShell";
type Data={taipei_zones:any[]};
export default function TaipeiZonesPage(){const{data,loading,error}=useClinicFetch<Data>("/api/clinic/taipei-zones");return <ClinicShell title="台北服務據點總覽" subtitle="只管理實際可服務據點：六張犁、西門、國父紀念館。板橋、三重、土城是來源區，不是服務據點。"><ClinicNotice loading={loading} error={error}/>{data&&<section className="bf-card bf-section-gap bf-table-wrap"><table className="bf-admin-table"><thead><tr><th>據點</th><th>狀態</th><th>常態據點</th><th>共享工作室</th><th>集中時段</th><th>房間成本</th><th>從家中移動</th><th>成本類型</th><th>備註</th></tr></thead><tbody>{data.taipei_zones.map(z=><tr key={z.zone_code}><td>{z.display_name_zh}</td><td>{z.status}</td><td>{z.is_regular_base?"是":"否"}</td><td>{z.is_rental_space?"是":"否"}</td><td>{z.requires_block_booking?"是":"否"}</td><td>NT${z.default_room_cost}</td><td>{z.default_travel_minutes_from_home} 分</td><td>{z.cost_type}</td><td>{z.notes}</td></tr>)}</tbody></table></section>}</ClinicShell>}
