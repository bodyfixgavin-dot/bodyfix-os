import Link from "next/link";
import { vedicChapters, vedicSupplementaryModules } from "@/data/learning/tracks";
export default function VedicLearningPage(){return <main className="portal-page"><div className="portal-detail-shell learning-shell"><Link className="portal-back-link" href="/learning">← 返回 Learning Portal</Link><header className="portal-detail-hero learning-hero"><p className="portal-kicker">CHART NAVIGATOR｜研究整理中</p><h1>Vedic Astrology Foundations</h1><p className="portal-detail-lead">本學習路徑以公開可見的課程主題作為初步研究骨架，內容將透過多來源重新研究、查證、改寫並補充案例。本頁並非亞東科技大學官方教材、授權課程或課堂筆記。</p><p className="method-emphasis">不得複製未提供的講義、教材、老師原文，亦不宣稱已修讀該課程。</p></header><section className="portal-detail-section learning-section"><div className="portal-detail-heading"><span>Eight Chapters</span><h2>八個核心章節</h2></div><div className="learning-module-list">{vedicChapters.map((chapter)=><article className="learning-module-card" key={chapter.id}><div><span>{chapter.status}</span><h3>{chapter.title}</h3><p>{chapter.summary}</p></div><ul>{chapter.items.map((item)=><li key={item}>{item}</li>)}</ul>{chapter.aliases ? <p className="method-note">別名欄位：{chapter.aliases.join("、")}</p> : null}{chapter.sourceNotes ? <p className="method-note">{chapter.sourceNotes.join(" ")}</p> : null}</article>)}</div></section><section className="portal-detail-section learning-section"><div className="portal-detail-heading"><span>Supplementary Modules</span><h2>正式知識系統預留補充模組</h2></div><div className="learning-track-grid">{vedicSupplementaryModules.map((mod)=><article className="method-card" key={mod.title}><span className="method-label">{mod.status}</span><h3>{mod.title}</h3><p>目前只預留架構，待多來源查證後補充。</p></article>)}</div><div className="method-system-note"><p>每個吠陀名詞的資料型別已預留：英文／IAST、天城文、中文名稱、中文發音空耳、核心定義、常見別名、來源、查證狀態、案例與教學說法。</p></div></section></div></main>}
